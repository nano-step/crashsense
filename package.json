{
  "name": "crashsense",
  "version": "1.0.0",
  "private": true,
  "description": "Crash diagnosis SDK for React, Vue, and mobile web — root cause analysis, not just error capture",
  "author": "hoainho <nhoxtvt@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nano-step/crashsense.git"
  },
  "homepage": "https://github.com/nano-step/crashsense#readme",
  "bugs": {
    "url": "https://github.com/nano-step/crashsense/issues"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npx vitest run",
    "lint": "tsc --noEmit --project tsconfig.json",
    "clean": "rm -rf packages/*/dist",
    "copy-readme": "for pkg in packages/*/; do cp README.md \"$pkg/README.md\"; done",
    "publish:all": "npm run copy-readme && npm run build && npm publish --workspaces --access public",
    "publish:github": "npm run copy-readme && npm run build && npm publish --workspaces --registry=https://npm.pkg.github.com",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^25.3.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "jsdom": "^28.1.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitepress": "^1.6.4",
    "vitest": "^4.0.18",
    "vue": "^3.5.28"
  }
}
